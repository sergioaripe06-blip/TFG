<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/appicon"
        android:roundIcon="@mipmap/appicon_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.FlatShare">
        <activity android:name="com.sergio.flatshare.features.groups.OwnerRoomsActivity" />
        <activity android:name="com.sergio.flatshare.features.auth.RegisterActivity" />
        <activity android:name="com.sergio.flatshare.features.auth.LoginActivity" />
        <activity android:name="com.sergio.flatshare.features.shell.MainActivity" />
        <receiver android:name="com.sergio.flatshare.features.reminders.ReminderReceiver" />
        <activity
            android:name="com.sergio.flatshare.features.shell.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>

